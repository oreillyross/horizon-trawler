# create a script which one can run every two hours using a cron job.
# or look at creating a serverless function again which can be run by serverless
# function.

# function which records begin time
# timesout after a set time (5secs or such)
# fetches articles from a news source based on keywords
# saves the metadata of articles on postgres database
# saves the text of article (strip images and hyperlinks, etc)
# in postgres database

# function takes a url and returns the html file url -> text [html file]

